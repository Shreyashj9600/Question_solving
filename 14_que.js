//What is Destructuring in Javascript ? 

// Destructuring is a JavaScript expression that allows us to extract data 
// from arrays, objects, and maps and set them into new, distinct variables.
// Destructuring allows us to extract multiple properties, or items, from an array​ at a time.

const obj = { name: "shreyash", age: 23, designation: "FrontEnd Developer" }

console.log(obj);
console.log(obj.name);
console.log(obj.age);
console.log(obj.designation);

